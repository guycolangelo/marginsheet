{
  "name": "@marginsheet/schema",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./conventions": "./src/conventions.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate:up": "tsx src/migrate.ts up",
    "migrate:down": "tsx src/migrate.ts down",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --dir test/unit --passWithNoTests",
    "test:introspection": "vitest run --dir test/db"
  },
  "dependencies": {
    "drizzle-orm": "^0.44",
    "postgres": "^3",
    "@marginsheet/shared": "workspace:*"
  },
  "devDependencies": {
    "@marginsheet/shared": "workspace:*",
    "@types/node": "^22",
    "drizzle-kit": "^0.31",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3"
  }
}
