[
  {
    "id": "sign-in-surface-styling",
    "item": "The /auth/confirm pages carry minimal placeholder styling. Design holds the sign-in surfaces; what ships now is legibility only and is not a design decision.",
    "owner": "Guy (Design)",
    "owed_to": "M8",
    "raised": "2026-08-16"
  },
  {
    "id": "postmark-test-mode",
    "item": "Postmark is in test mode pending approval, so production sends are limited to @marginsheet.com recipients. The live send to guy@astonishent.com is still owed once it clears.",
    "owner": "Guy",
    "owed_to": "Postmark account approval",
    "raised": "2026-08-16"
  },
  {
    "id": "twilio-trial-caller-id",
    "item": "The Twilio account is a trial, so Verify reaches only allowlisted numbers. The refusal PATH is now built and tested against a refusing sender (honest message naming the number, no hang, no 500), and it is permanent because Twilio refuses numbers for reasons that outlive the trial. What is still owed is one LIVE send to a non-allowlisted number, because only the real account produces the real error body.",
    "owner": "Guy",
    "owed_to": "TRIGGER: A2P 10DLC clearance, late Aug 2026 at the earliest, then a live send by Guy.",
    "raised": "2026-08-16",
    "trigger": "A2P 10DLC clearance, late August 2026 at the earliest"
  },
  {
    "id": "provider-events-handler-half",
    "item": "Invariant 5's handler half is unbuilt. It now covers four providers rather than three, since Postmark bounce and complaint webhooks joined Plaid, Stripe and Twilio in provider_events (amendment 7).",
    "owner": "build",
    "owed_to": "M4 and M7",
    "raised": "2026-08-16"
  },
  {
    "id": "canon-fixtures",
    "item": "Five canon fixtures are owed before the golden suite can judge composed output against them. FraudReply first.",
    "owner": "Guy",
    "owed_to": "M10",
    "raised": "2026-08-16"
  },
  {
    "id": "rotation-guard-declaration",
    "item": "The role-rotation guard trusts the caller's declaration of the target branch. NEON_TEST_BRANCH=pr-1 aimed at dev would still rotate dev. Verifying the declaration against Neon's branch endpoint would close it.",
    "owner": "build",
    "owed_to": "unscheduled",
    "raised": "2026-08-16"
  },
  {
    "id": "journey-copy-review",
    "item": "Trademark placement is a copy-review item rather than lint (ruled 16 Aug 2026), because the unit of first use is a journey and a linter cannot see one. That makes it depend on a journey copy review, which is not scheduled anywhere yet. The sign-in journey is the first that needs one.",
    "owner": "Guy (Design)",
    "owed_to": "M8, with the sign-in surfaces",
    "raised": "2026-08-16"
  },
  {
    "id": "edge-rate-limit-rule",
    "item": "The per-source magic-link edge rule is deployed at the free plan's fixed shape: 10 requests per 10 seconds, ip.src only, no cf.colo.id, mitigation timeout not exposed. That is a burst brake rather than a sustained-rate control, so the per-email limit and the global ceiling in our own code are load-bearing rather than secondary. Revisit on any plan upgrade: 60 second period, cf.colo.id as a second characteristic, 600 second mitigation.",
    "owner": "Guy",
    "owed_to": "a Cloudflare plan upgrade",
    "raised": "2026-08-16"
  },
  {
    "id": "recovery-twilio-credentials",
    "item": "The recovery path fails closed without Twilio credentials: with no OTP sender the phone half cannot be met, so /auth/recovery returns 503 rather than completing on one factor. Correct behaviour, and it means recovery is NOT USABLE IN ANY ENVIRONMENT until Twilio credentials land. TIMELINE CORRECTED 18 Aug 2026 (Guy): A2P clearance is LATE AUGUST AT THE EARLIEST. The EIN was registered 12 Aug and brand registration needs it propagated into the verification databases first, so this is at least another week. THE ORIGINAL TRIGGER OF '3.3' IS MISLEADING AND IS CORRECTED HERE: 3.3 built the path, and the path has never run, and it stays that way longer than a module boundary implies. Anything that assumes SMS is available should be read against this date rather than against 3.3 being done.",
    "owner": "Guy",
    "owed_to": "TRIGGER: A2P 10DLC clearance, late Aug 2026 at the earliest. NOT 3.3, which only built the path.",
    "raised": "2026-08-17",
    "trigger": "A2P 10DLC clearance, late August 2026 at the earliest"
  },
  {
    "id": "second-credential-write-path",
    "item": "Recovery writes passkey credentials directly rather than through @better-auth/passkey, because the plugin requires a session and \u00a71 says the session exists only after the credential. Two paths writing the same shape is a drift surface. A THIRD is a ruling, not an implementation detail. Drift is caught by the named assertion that a recovery-registered passkey must AUTHENTICATE through the plugin.",
    "owner": "build",
    "owed_to": "standing constraint, reviewed whenever @better-auth/passkey is upgraded",
    "raised": "2026-08-17"
  },
  {
    "id": "rule-2-refusal-timing",
    "item": "RULED ACCEPTABLE, 17 Aug 2026 (Guy), and recorded so it is not revisited as an oversight. Rule 2's cross-household collision is caught at CONFIRM rather than START, because members carries household_isolation and a pre-check cannot see another household's row. The constraint enforces; the code translates the violation. Cost: one wasted SMS and a clear refusal. Weighed against a second SECURITY DEFINER function, which is a permanent widening of the RLS hole, for a case that is genuinely rare: two households legitimately claiming one verified number is either a typo or a person moving between households, and both want support anyway.",
    "owner": "closed by ruling",
    "owed_to": "nothing; recorded so the trade is not re-opened as a defect",
    "raised": "2026-08-17"
  },
  {
    "id": "sensitive-actions-unbuilt",
    "item": "Three of the four sensitive actions \u00a71 names do not exist yet: cancellation (M7), member removal (3.5), export (M8). They are enumerated in services/api/src/sensitive-actions.ts with built:false, and a test asserts each has NO route, so building one without flipping the flag fails and sends the author to where the recent-auth requirement is written. Recent-auth is live on the one that exists.",
    "owner": "M7, 3.5 and M8 respectively",
    "owed_to": "each action's own module",
    "raised": "2026-08-17"
  },
  {
    "id": "planted-failure-inertness",
    "item": "The planted-failure harness proves a mutation changed the FILE, not that it changed BEHAVIOUR, and proving the second is undecidable in general. So a 'test did not go red' result has two causes it cannot distinguish: an insensitive control, or an inert mutation. One was already found this way: a mutation whose injected condition was always undefined. The failure message names both causes, and register mutations need reviewing as code rather than as data.",
    "owner": "build",
    "owed_to": "standing constraint, reviewed whenever a register entry is added",
    "raised": "2026-08-17"
  },
  {
    "id": "sandbox-cannot-build-four-of-five-fixtures",
    "item": "Invariant 8 lists five Sandbox error fixtures and FOUR CANNOT BE CONSTRUCTED (spike 1d, 17 Aug 2026). Constructible: ITEM_LOGIN_REQUIRED via /sandbox/item/reset_login. Not constructible: item error (webhook_type has no code ERROR), removed transactions (TRANSACTIONS_REMOVED is not fireable and the removed stream was empty in every run), pending to posted (0 pending across 48 default-user transactions and across user_custom shapes), reversal as an event. The invariant is rewritten to claim what Sandbox can actually prove; the rest are gaps rather than tests written against fixtures that cannot fail.",
    "owner": "build",
    "owed_to": "M4, and invariant 9 at M9 for the states only real Items produce",
    "raised": "2026-08-17"
  },
  {
    "id": "pending-to-posted-unexercised",
    "item": "THE ONE TRACKED LOUDEST (Guy, 17 Aug 2026). Pending to posted is NOT an error path. It is the normal daily behaviour of every card transaction in the product, and categorization-spec section 10 turns on it. Plaid Sandbox cannot construct it: zero pending transactions across 48 default-user rows and across every user_custom shape tried, so no CI fixture can express the transition. M4 tests the transition against a hand-built fixture of our own rows, which proves our filing and reconciliation logic and DOES NOT prove Plaid's shape is what we assumed. Those are two different claims and only the first is made. THE SECOND CAN ONLY BE PROVEN IN ONE PLACE: invariant 9, against the founder household's real institutions. A silent double-count on this transition reaches a household's Kept figure before anyone sees it. RESEQUENCED 17 Aug 2026: the founder household's real institutions connect at 4.5b, EARLY in M4, rather than at the end. Finding that our filing logic mishandles a settling transaction in week one of M4 is a fix; finding it at invariant 9 with the module otherwise done is a rewrite. Connecting early exercises section 10 during the build and does NOT discharge invariant 9, which still needs a full backfill across every founder institution as M9's precondition.",
    "owner": "Guy",
    "owed_to": "4.5b for the real settling transaction, invariant 9 at M9 for the full backfill",
    "raised": "2026-08-17"
  },
  {
    "id": "historical-update-not-fireable",
    "item": "plaid-pipeline-spec section 4 gives HISTORICAL_UPDATE real work: it marks backfill-complete for the M13 intro trigger. Sandbox will not fire it (INVALID_FIELD), so the webhook half of that trigger cannot be exercised in CI. The trigger's 30 minute fallback and the 'I have connected all my accounts' button remain testable; the webhook path does not.",
    "owner": "build",
    "owed_to": "M13, flagged from M4",
    "raised": "2026-08-17"
  },
  {
    "id": "sandbox-transactions-create-is-inert",
    "item": "/sandbox/transactions/create returns 200 and changes nothing on a default-user Item, and returns 400 on a user_custom Item. Proven by looking for a marker description rather than by counting, because Sandbox generates transactions on its own and a count cannot tell a mutation from natural generation. An endpoint that reports success and does nothing is the same shape as a control that cannot fail. Nothing in M4 may build a fixture on it.",
    "owner": "build",
    "owed_to": "standing constraint on M4 fixtures",
    "raised": "2026-08-17"
  },
  {
    "id": "sync-mutation-branch-not-deterministic",
    "item": "TRANSACTIONS_SYNC_MUTATION_DURING_PAGINATION is real and cannot be produced on demand. Plaid refuses a mid-pagination cursor when the underlying data changes, which is what a webhook landing mid-sync does, so the pipeline persists TWO cursors: the in-flight one for a crash, and the last-completed-sync one as the fallback on that error. Reproduced 3 times out of 5 against freshly created Sandbox Items still generating transactions, which is flaky and therefore not a fixture. M4 tests the HANDLER against a synthesised 400 and does not claim to have proven Plaid's behaviour; the live observation is the evidence for the branch existing.",
    "owner": "build",
    "owed_to": "M4 for the handler test, invariant 9 at M9 for the live behaviour",
    "raised": "2026-08-17"
  },
  {
    "id": "production-plaid-credentials",
    "item": "IN GUY'S HANDS AND READY FOR THE PASTE, confirmed 18 Aug 2026. Held deliberately rather than outstanding. Not a blocker to chase: the approved sequence is Sandbox green through 4.5, then the real connection at 4.5b, then 4.6 through 4.9 built against both. Claude flags on reaching 4.5b and Guy runs the paste session, PRODUCTION ONLY. Dev and staging keep their Sandbox pair. PREREQUISITE THAT IS NOT YET DONE, and it changes where the paste goes: Plaid credentials are currently declared and stored on api/dev and api/staging, and the custody doc already says they belong to SYNC after the third-Worker ruling, because the exchange happens where the encryption key lives. sync declares no Plaid credentials in any environment. So the paste at 4.5b targets marginsheet-sync production, NOT api, and config/worker-secrets.json must declare it there first or the secret-inventory job refuses the paste as an undeclared secret. The dev and staging move from api to sync is owed alongside it.",
    "owner": "Guy",
    "owed_to": "TRIGGER: Claude FLAGS on reaching 4.5b, then Guy runs the paste session. PRODUCTION ENVIRONMENT ONLY.",
    "raised": "2026-08-17",
    "trigger": "Claude flags on reaching 4.5b; Guy runs the paste session"
  },
  {
    "id": "auth-is-production-only",
    "item": "BETTER_AUTH_SECRET, BETTER_AUTH_URL and AUTH_FROM_EMAIL exist on api/production and on NEITHER dev nor staging, and they are not vars in wrangler.jsonc either. Found while writing config/worker-secrets.json (4.2.3). Guy's read, 17 Aug 2026: dev and staging genuinely do not serve auth, because nothing has ever signed in there and the magic link path was only ever proven against production. NOT BLESSED INTO THE DECLARATION, because a declaration written from current state agrees with whatever was done. Two ways to close it: either those environments get their own auth config, or we state plainly that auth is production-only and accept that its tests never run outside production. The second is a real cost worth naming rather than discovering: it means every auth control is exercised in exactly one environment, and that environment is the one with households in it.",
    "owner": "Guy",
    "owed_to": "a ruling, then either config or a stated limitation",
    "raised": "2026-08-17"
  },
  {
    "id": "canon-revision-two-registers-one-voice",
    "item": "THE SINGLE ASSISTANT RULING'S CANON CONSEQUENCE, owed since 15 Aug 2026 and recorded only in CLAUDE.md prose until 18 Aug. The conversational spec's two registers become two MODES of one voice. That is a canon revision, not a rename: MyKeeper is the household's only named assistant, the brains stay architecturally separate behind it, and the advice gate stays enforced on advisory output exactly as specified. Until this is written, the conversational spec still describes two staff a household chooses between. TRIGGERED BY THE DESIGN TRACK COMPLETING, not by a module number (Guy, 18 Aug 2026). The reason is that the revision needs the voice to have a face: once MyKeeper has been designed as one assistant across every surface, the mode question is concrete rather than abstract. HARD DEADLINE M12, which builds golden fixtures from the canon. If the canon still describes two voices when the fixtures are written, THE FIXTURES ENCODE TWO VOICES AND THE JUDGE GETS TUNED AGAINST THEM, which is far more expensive to undo than a spec edit.",
    "owner": "Guy",
    "owed_to": "TRIGGER: the design track completing. HARD DEADLINE: M12.",
    "raised": "2026-08-15",
    "trigger": "the design track completing"
  },
  {
    "id": "article-as-answer-m2-amendment",
    "item": "The article-as-answer pattern is owed as an M2 amendment with its own plan (Guy, 15 Aug 2026), carrying two requirements. 1. The article's identity is a STABLE SLUG, never its URL or title, both of which change when an article is edited or renamed; the reference block carries the slug and resolves title and URL at assembly time, so a renamed article never strands a citation inside an artifact already sent. 2. The gate rule ships as a FIXTURE PAIR, not only as a rule: one passing example (cards ranked by rate, reference attached, no action named) and one failing example (the same list framed as 'here is where to start'). Recorded in CLAUDE.md prose since 15 Aug and tracked by nothing until now.",
    "owner": "Guy",
    "owed_to": "an M2 amendment plan",
    "raised": "2026-08-15"
  },
  {
    "id": "cross-brain-attribution-fact-package-review",
    "item": "Consequence 5 of the single assistant ruling: any fact-package class existing ONLY to carry cross-brain attribution is reviewed and REPORTED, never silently deleted. Nothing has performed that review. It matters because the classes are the seam between the two brains, and deleting one quietly would remove the evidence of what the two-staff model needed.",
    "owner": "build",
    "owed_to": "M2, reported to Guy",
    "raised": "2026-08-15"
  },
  {
    "id": "conversation-spec-describes-dead-platforms",
    "item": "SURVEYED 18 Aug 2026, not rewritten. SCOPE: THE ARCHITECTURE SECTIONS ONLY, being the Base44 seam and the D1 references. Not the doctrine, not the voice, not the registers. conversation-service-spec.md assumes TWO platforms that no longer exist. BASE44, 13 references, structural rather than incidental: section 7 (reads and writes household data through Base44's API; Base44 keeps the CRUD app and the sync pipeline), 14 (the Base44 data access layer), 15 (durable knowledge lives in Base44), 16 (Base44 API token as a secret), 106 to 110 (an entire section titled 'Task zero: the Base44 seam'), 118 (tag creation and membership write to Base44), 180 (MyCFO month awareness gated on build 3, which lives in Base44), 184 (every Plaid sync landing in Base44 fires household-state-changed), 514, 522 and 532. D1, 10 references, as the store for conversation state: sections 15, 36 (thread expiry via a D1 state check), 42 (members as first-class correspondents in D1), 118 (standing instructions), 168 (tombstones), 188 (condition-state memory), 337 (handoff records), 409 (session_offer). The locked stack is Neon Postgres plus Durable Objects, and data-model-spec invariant 8 says in terms 'this is why D1 died'. SECTION 514 IS THE DANGEROUS ONE AND IS CALLED OUT BY NAME. It states that all Base44 writes go through the same validated endpoints the app uses and that the service holds no direct database access. That is the opposite of the current architecture, and CRITICALLY IT READS AS A CONSTRAINT RATHER THAN A DESCRIPTION: someone building M13 would treat it as a security property to preserve and would architect around a rule that no longer applies. The other references read as stale; this one reads as a requirement. M10's channel plumbing touches the D1 assumptions directly, which is why this is owed before PHASE B opens rather than before M10 specifically. NO PLATFORM WORK IS NEEDED ON mycfo-mykeeper-conversational-spec.md. It contains ZERO references to Base44 or D1. The voice, the doctrine, the registers and the advice gate are platform independent and survive intact. RECORDED SO NOBODY RE-SURVEYS IT LATER.",
    "owner": "Guy",
    "owed_to": "TRIGGER: M9 completing, so it lands BEFORE M10 opens rather than during it.",
    "raised": "2026-08-18",
    "trigger": "M9 completing"
  },
  {
    "id": "plaid-rate-limit-shape-uncaptured",
    "item": "Seven Plaid error classes were captured from Sandbox for invariant 7 and RATE LIMITING IS NOT AMONG THEM. Sandbox did not produce a 429 under normal use, and hammering it to force one is a poor trade for one data point. NAMED AS A GAP RATHER THAN SYNTHESISED (Guy, 18 Aug 2026): a fabricated 429 would prove our handler copes with the shape we imagined. WHY THIS PARTICULAR GAP MATTERS MORE THAN THE OTHER SIX: a 429 is the class most likely to arrive with a retry envelope, and a retry envelope is the exact shape that would carry the request, and the request is where the access token is. The one class we could not capture is the one whose shape the amended invariant 7 most wants to see. First real 429 in production is the moment to capture it.",
    "owner": "build",
    "trigger": "the first real 429 observed from Plaid, in any environment",
    "owed_to": "TRIGGER: the first real 429 observed from Plaid. Capture the body then; do not synthesise one before.",
    "raised": "2026-08-18"
  },
  {
    "id": "account-overlap-recorded-not-acted-on",
    "item": "RULED 18 Aug 2026. A household completing Link twice produces two Items at one institution and two bills, and so does a household connecting a legitimate second login. Nothing distinguishes them from Plaid's side. THE CONTROL FAILS TOWARD PERMITTING, on which error is recoverable: a false refusal blocks a household from connecting their own business account with NO PATH AROUND IT, and a duplicate costs a few dollars a month and is fixable the moment anyone notices. Asymmetric, so allow. ACCOUNT-MASK OVERLAP IS A SIGNAL, NOT A RULE: two Items sharing every mask is almost certainly one login connected twice, two sharing none is almost certainly two logins, and the middle is genuinely ambiguous which is exactly where a rule would be wrong. M4 stores the overlap and surfaces it as an internal item for a human to read, and acts on it nowhere.",
    "owner": "build",
    "trigger": "when insight_ledger is first written, at 4.6",
    "owed_to": "TRIGGER: 4.6, when insight_ledger is first written and there is somewhere to put an internal item.",
    "raised": "2026-08-18"
  },
  {
    "id": "ask-at-connect-when-overlap-ambiguous",
    "item": "The honest long answer to distinguishing a second login from a double connect is ASKING THE HOUSEHOLD, and it belongs in M8 where Link's flow lives. WITH ONE CONSTRAINT THAT IS THE WHOLE POINT (Guy, 18 Aug 2026): the question is only worth asking when the overlap signal is AMBIGUOUS. Two Items sharing every account mask, or sharing none, need no question. Asking every household to explain their own bank accounts is the setup cliff the product refuses, and a question asked always is a question that teaches people to dismiss it.",
    "owner": "Guy (Design) and M8",
    "trigger": "M8's Link surface",
    "owed_to": "TRIGGER: M8's Link surface. Asked ONLY on an ambiguous overlap, never by default.",
    "raised": "2026-08-18"
  },
  {
    "id": "capital-one-link-parameters",
    "item": "The ported Capital One parameter handling is a LINK-CONFIG note, per plaid-pipeline-spec section 2, and Link's configuration is M8's surface rather than M4's exchange. Carried to M8 rather than closed silently at 4.3.5. What it is: Capital One requires specific Link parameters that Base44 discovered by hitting the failure, and the fix was ported once. Nobody has re-derived WHY, so M8 should confirm the requirement against Plaid's current docs rather than copying a workaround whose reason is unrecorded.",
    "owner": "M8",
    "trigger": "M8's Link surface",
    "owed_to": "TRIGGER: M8's Link surface, with the requirement re-derived rather than copied.",
    "raised": "2026-08-18"
  },
  {
    "id": "exchange-idempotency-has-no-planted-failure",
    "item": "Exchange idempotency is guarded by a DATABASE CONSTRAINT rather than by our code: the unique index on plaid_items.item_id, plus Plaid's own idempotency on the public token, proven by spike 18 Aug 2026. That is the stronger arrangement and it leaves a gap in the register. The test asserts the migration FILE contains the index, so a SQL mutation dropping the index would not redden it, and mutating the migration file is forbidden by migrations-append-only. So this control has NO usable planted failure and is deliberately unregistered rather than registered with a mutation that proves nothing. Closing it needs a test that queries pg_indexes against the live branch, which can then be reddened by a DROP INDEX in a transaction.",
    "owner": "build",
    "trigger": "when a database-backed sync test suite exists, likely 4.4",
    "owed_to": "TRIGGER: a database-backed sync suite, likely at 4.4. Registered when the mutation can be real.",
    "raised": "2026-08-18"
  },
  {
    "id": "time-based-watcher-half-unbuilt",
    "item": "THE WATCHER HAS TWO INPUTS AND M4 BUILDS ONE (Guy, 18 Aug 2026). household-state-changed is the EVENT-DRIVEN half, firing when data moves. The TIME-BASED half is a daily Cron pass: an expected commitment missed, an income stream missed, a date passing. A rule that fires because TIME moved cannot fire from a signal that fires when DATA moves, so building only the signal leaves every time-based rule PERMANENTLY SILENT rather than merely late, which is the worse of the two. Recorded so a green M4 is not read as a working watcher: THE SIGNAL EXISTING IS NOT THE WATCHER EXISTING, and the half M4 ships cannot cover the half it does not.",
    "owner": "build",
    "trigger": "M13, with the watcher itself",
    "owed_to": "TRIGGER: M13, with the watcher. M4 ships the signal only, and that is half the input.",
    "raised": "2026-08-18"
  },
  {
    "id": "sms-dependent-work-blocked-until-a2p",
    "item": "ANYTHING ASSUMING SMS IS UNAVAILABLE UNTIL LATE AUGUST 2026 AT THE EARLIEST (Guy, 18 Aug 2026). A2P 10DLC brand registration is waiting on the EIN registered 12 Aug to propagate into the verification databases. This does not touch M4. It DOES touch M10's channel work and the recovery path, and the risk is that a module boundary reads as a date: recovery-twilio-credentials was triggered on '3.3', which made it look resolved once 3.3 shipped, when 3.3 only built a path that has never run. Recorded separately so the dependency is visible from the outside rather than only inside the items it constrains.",
    "owner": "Guy",
    "trigger": "A2P 10DLC clearance",
    "owed_to": "TRIGGER: A2P 10DLC clearance. Read any SMS assumption against this rather than against a module number.",
    "raised": "2026-08-18"
  },
  {
    "id": "soc2-posture",
    "item": "DECIDED 18 Aug 2026: NOT PURSUING SOC 2 NOW. Pre-revenue, one person, an unusual stack, and no customer requiring it. Realistic first-year cost 20 to 35 thousand dollars across platform, auditor and pen test. Recorded as a DECISION rather than a gap, so it is not rediscovered as an oversight. TWO THINGS THAT MAKE THE DEFERRAL CHEAPER THAN IT LOOKS. The Written Infosec Program is already owed to Plaid and is the foundation of a SOC 2 posture, so it is worth writing regardless and is not wasted work if this is never pursued. And the change management and access control evidence this project already produces as a matter of course, every change through a pull request with required checks, branch protection with no admin override, enumerated role grants, a control register with planted failures, WOULD COVER A TYPE II OBSERVATION WINDOW RETROACTIVELY if it ever matters. The observation window is the expensive part of a Type II and the only part that cannot be bought later, so producing the evidence now without pursuing the audit is the arrangement that keeps the option open at no cost.",
    "owner": "Guy",
    "trigger": "the first customer, partner or investor who requires it",
    "owed_to": "TRIGGER: the first customer, partner or investor who REQUIRES it. Not a module and not a date: per the trigger rule, this is gated on someone outside the project.",
    "raised": "2026-08-18"
  },
  {
    "id": "retract-workers-dev-staging-production",
    "item": "STAGING AND PRODUCTION marginsheet-sync AND marginsheet-conversation ARE STILL REACHABLE ON workers.dev, and the repo fix does not retract them by itself. Verified 19 Aug 2026: with workers_dev false in wrangler.jsonc, `wrangler deploy --env staging` uploads a version and then prints `No targets deployed`, leaving the ALREADY PUBLISHED workers.dev route serving the old version. Dev went silent from the same change; the named environments did not. WHAT IS EXPOSED WHILE THIS IS OPEN: marginsheet-sync holds TOKEN_ENCRYPTION_KEY, and in production /health and /debug/crypto-selftest answer 200 to anyone, while POST /internal/exchange answers 400 to an empty body, which is the handler rejecting the body rather than the route being absent. TWO THINGS ARE NEEDED AND NEITHER IS CLAUDE'S TO DO: the production deploy is Guy's or CI's, and retracting an already-published subdomain looks like it needs the Cloudflare API rather than a deploy, which is an account settings change. DO NOT GUESS WHICH: the enumerating-causes rule applies, so probe the subdomain setting for one script and report what it says before changing anything.",
    "owner": "Guy",
    "trigger": "immediately, and BEFORE 4.5b: production Plaid credentials must not land on a Worker that answers the public internet",
    "owed_to": "TRIGGER: now. Raised 19 Aug 2026 while probing for a Plaid webhook payload. The repo-side fix and both controls are on the branch; what remains is deploy and subdomain retraction.",
    "raised": "2026-08-19"
  },
  {
    "id": "cross-household-upsert-overwrites-another-household",
    "item": "CONFIRMED 19 AUG 2026 AGAINST A REAL DATABASE: one household's upsert overwrites another household's plaid_items row. Acting as household A with marginsheet.household_id set to A, verified by reading current_setting back inside the transaction, the statement exchange.ts issues (`insert into plaid_items ... on conflict (item_id) do update set access_token_ciphertext = excluded...`) SUCCEEDED against a row owned by household B and replaced B's access_token_ciphertext. Ownership stayed with B only because household_id is not in the SET list. household_isolation did not refuse it. THE ROUTE IS ORDINARY USE, NOT AN ATTACK: item_id is Plaid's, and two households linking THE SAME joint account produce the same item_id, so this arrives through a shared bank login rather than through a guessed UUID. Consequences: B's Plaid token is destroyed so B's syncs break, and B's row then holds a token minted for A, which is a cross-household data path into B's ledger the next time sync runs. The same shape applies to financial_accounts.plaid_account_id, which carries the same global unique index; that half is NOT yet proven, because the test exercising it had a NOT NULL omission that made it pass vacuously. NOT FIXED IN THE CURRENT CHAIN, by Guy's ruling: it is a schema-level finding whose fix touches migrations, the exchange path and the account-overlap item, and it needs its own plan rather than being folded into a PR about something else. The proving test and its register entry are preserved on branch finding/cross-household-upsert; they were removed from the authorization PR because the finding is real and would otherwise hold a live security fix behind a red check.",
    "owner": "build, gated on Guy's approval of the plan",
    "trigger": "BEFORE A SECOND HOUSEHOLD CONNECTS ANYTHING, IN ANY ENVIRONMENT. Not a module number: 4.5b is when real Items first exist, but the condition that produces this is a second household linking an account, which can happen in dev or staging or twice on Guy's own logins, and a module completing says nothing about whether that has occurred. The trigger is the condition, not the milestone.",
    "owed_to": "TRIGGER: the condition above. Confirmed 19 Aug 2026 by the harness with the GUC read back inside the transaction, which is what separated a genuine cross-household write from an unrestored planted mutation. THE PLAN IS THE BUILD'S TO DRAFT AND GUY'S TO APPROVE, same as every other task; it was first recorded as owned by Guy, which would have left him owning a plan he was never going to write. Evidence lives on branch finding/cross-household-upsert and PR #113.",
    "raised": "2026-08-19"
  }
]
