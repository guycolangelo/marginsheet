{
  "name": "@marginsheet/api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --exclude '**/isolation.test.ts' --exclude '**/db-identity.test.ts' --passWithNoTests",
    "test:isolation": "vitest run test/isolation.test.ts",
    "test:db-identity": "vitest run test/db-identity.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "@types/node": "^22",
    "postgres": "^3",
    "typescript": "^5",
    "vitest": "^3",
    "wrangler": "^4"
  },
  "dependencies": {
    "@marginsheet/shared": "workspace:*",
    "@sentry/cloudflare": "^10",
    "postgres": "^3"
  }
}
