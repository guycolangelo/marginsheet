{
  "_doc": [
    "Which secrets each Worker is PERMITTED to hold, per environment (M4 task 4.2.3).",
    "",
    "WHY. M4 section 2a rules that the Plaid token is decrypted only inside a sync",
    "Worker with no public routes. That ruling is undone by one secret nobody",
    "deleted: a key present on the sync Worker AND still on api is not a boundary,",
    "and NOTHING FAILS if api keeps its copy. Every sync test passes either way.",
    "So the removal half gets its own proof, and this file is its expectation.",
    "",
    "IT LANDS BEFORE THE MOVE, DELIBERATELY. A check written after the change it",
    "verifies agrees with whatever was done. Written first it is RED on the",
    "un-removed key, and the move is what makes it green. Same reasoning as the",
    "planted failures: a control nobody has watched fail is not one.",
    "",
    "THE JOB FAILS IN BOTH DIRECTIONS, and keeps 'differs' distinct from 'could",
    "not read'. Those look identical from a green tick and mean opposite things.",
    "",
    "Names only. Wrangler never returns secret values and neither does the job."
  ],
  "workers": {
    "api": {
      "dev": [
        "NEON_DATABASE_URL",
        "PLAID_CLIENT_ID",
        "PLAID_SECRET",
        "STRIPE_SECRET"
      ],
      "staging": [
        "NEON_DATABASE_URL",
        "PLAID_CLIENT_ID",
        "PLAID_SECRET",
        "STRIPE_SECRET"
      ],
      "production": [
        "AUTH_FROM_EMAIL",
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ]
    },
    "conversation": {
      "dev": [
        "ANTHROPIC_API_KEY",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ],
      "staging": [
        "ANTHROPIC_API_KEY",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ],
      "production": [
        "NEON_DATABASE_URL"
      ]
    },
    "sync": {
      "dev": [
        "NEON_DATABASE_URL",
        "TOKEN_ENCRYPTION_KEY"
      ],
      "staging": [
        "NEON_DATABASE_URL",
        "TOKEN_ENCRYPTION_KEY"
      ],
      "production": [
        "NEON_DATABASE_URL",
        "TOKEN_ENCRYPTION_KEY"
      ]
    }
  },
  "expectedFailures": {
    "_doc": [
      "Entries here are KNOWN RED and the job still fails on them. They are not",
      "exemptions and there is no mechanism to silence one: this block exists so",
      "the failure message can say WHY a difference is expected and what closes",
      "it, rather than a reader concluding the job is broken."
    ],
    "api.dev.TOKEN_ENCRYPTION_KEY": "Removed by task 4.2.4, once marginsheet-sync exists to hold it. Declared absent here BEFORE the move so this job is red until the move happens.",
    "api.staging.TOKEN_ENCRYPTION_KEY": "As above.",
    "api.production.TOKEN_ENCRYPTION_KEY": "As above. This is the one that matters: production api has public routes and holds the key that decrypts every household's Plaid token.",
    "sync.dev.NEON_DATABASE_URL": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it.",
    "sync.dev.TOKEN_ENCRYPTION_KEY": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it.",
    "sync.staging.NEON_DATABASE_URL": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it.",
    "sync.staging.TOKEN_ENCRYPTION_KEY": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it.",
    "sync.production.NEON_DATABASE_URL": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it.",
    "sync.production.TOKEN_ENCRYPTION_KEY": "Declared before it exists. The sync Worker is created by 4.2.1's deploy and its secrets are set by 4.2.4, in that order. Red until then, deliberately: the expectation predates the change so it cannot have been written to agree with it."
  },
  "openQuestions": {
    "_doc": [
      "Found while writing this file, which is the point of writing it down.",
      "Recorded as questions rather than blessed into the declaration above."
    ],
    "auth-secrets-production-only": "BETTER_AUTH_SECRET, BETTER_AUTH_URL and AUTH_FROM_EMAIL exist on api/production and on NEITHER dev nor staging, and they are not vars in wrangler.jsonc either. Guy's read, 17 Aug 2026: those environments genuinely do not serve auth, because nothing has ever signed in there and the magic link path was only proven against production. DELIBERATELY NOT BLESSED INTO THE DECLARATION ABOVE. Tracked as auth-is-production-only in docs/open-items.json, owner Guy: either dev and staging get their own auth config, or we state plainly that auth is production-only and accept that its tests never run outside production."
  }
}
