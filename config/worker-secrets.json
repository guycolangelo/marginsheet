{
  "_doc": [
    "Which secrets each Worker is PERMITTED to hold, per environment (M4 task 4.2).",
    "",
    "WHY. M4 section 2a rules that the Plaid token is decrypted only inside a sync",
    "Worker with no public routes. That ruling is undone by one secret nobody",
    "deleted: a key present on the sync Worker AND still on api is not a boundary,",
    "and NOTHING FAILS if api keeps its copy. Every sync test passes either way.",
    "So the removal half got its own proof, and this file is its expectation.",
    "",
    "IT LANDED BEFORE THE MOVE, DELIBERATELY. A check written after the change it",
    "verifies agrees with whatever was done. Written first it was RED on the",
    "un-removed key in all three environments, and the move at 4.2.4 is what made",
    "it green. Same reasoning as the planted failures: a control nobody has",
    "watched fail is not one. This one was watched failing for its whole life",
    "before it ever passed.",
    "",
    "THE JOB FAILS IN BOTH DIRECTIONS, and keeps 'differs' distinct from 'could",
    "not read'. Those look identical from a green tick and mean opposite things,",
    "and the difference was load-bearing during the move: sync/production read as",
    "COULD NOT READ before it was deployed and as '2 missing' after, which are two",
    "different facts that a lesser check would have reported identically.",
    "",
    "Names only. Wrangler never returns secret values and neither does the job.",
    "",
    "ADDING A SECRET: add it here in the same commit. An undeclared secret fails",
    "this job, which is the point."
  ],
  "workers": {
    "api": {
      "dev": [
        "NEON_DATABASE_URL",
        "STRIPE_SECRET"
      ],
      "staging": [
        "NEON_DATABASE_URL",
        "STRIPE_SECRET"
      ],
      "production": [
        "AUTH_FROM_EMAIL",
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ]
    },
    "conversation": {
      "dev": [
        "ANTHROPIC_API_KEY",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ],
      "staging": [
        "ANTHROPIC_API_KEY",
        "NEON_DATABASE_URL",
        "POSTMARK_TOKEN"
      ],
      "production": [
        "NEON_DATABASE_URL"
      ]
    },
    "sync": {
      "dev": [
        "NEON_DATABASE_URL",
        "PLAID_CLIENT_ID",
        "PLAID_SECRET",
        "TOKEN_ENCRYPTION_KEY"
      ],
      "staging": [
        "NEON_DATABASE_URL",
        "PLAID_CLIENT_ID",
        "PLAID_SECRET",
        "TOKEN_ENCRYPTION_KEY"
      ],
      "production": [
        "NEON_DATABASE_URL",
        "TOKEN_ENCRYPTION_KEY"
      ]
    }
  },
  "openQuestions": {
    "_doc": [
      "Found while writing this file, which is the point of writing it down.",
      "Recorded as questions rather than blessed into the declaration above."
    ],
    "auth-secrets-production-only": "BETTER_AUTH_SECRET, BETTER_AUTH_URL and AUTH_FROM_EMAIL exist on api/production and on NEITHER dev nor staging, and they are not vars in wrangler.jsonc either. Guy's read, 17 Aug 2026: those environments genuinely do not serve auth, because nothing has ever signed in there and the magic link path was only proven against production. DELIBERATELY NOT BLESSED INTO THE DECLARATION ABOVE. Tracked as auth-is-production-only in docs/open-items.json, owner Guy: either dev and staging get their own auth config, or we state plainly that auth is production-only and accept that its tests never run outside production."
  },
  "expectedFailures": {
    "_doc": [
      "KNOWN RED AND STILL FAILING. Not exemptions and there is no mechanism to",
      "silence one. This block exists so the failure message can say WHY a",
      "difference is expected and what closes it.",
      "",
      "Declared BEFORE the move, deliberately, the same as 4.2.3: a check written",
      "after the change agrees with whatever was done."
    ],
    "api.staging.PLAID_CLIENT_ID": "Removed by task 4.3.1. Plaid credentials mint access tokens, and after the third-Worker ruling the exchange happens where the encryption key lives. Red until the move.",
    "sync.staging.PLAID_CLIENT_ID": "Set by task 4.3.1, after api's copy is proven gone for that environment. Red until then.",
    "api.staging.PLAID_SECRET": "Removed by task 4.3.1. Plaid credentials mint access tokens, and after the third-Worker ruling the exchange happens where the encryption key lives. Red until the move.",
    "sync.staging.PLAID_SECRET": "Set by task 4.3.1, after api's copy is proven gone for that environment. Red until then."
  }
}
